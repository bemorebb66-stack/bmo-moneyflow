# Telemetry is deliberately off unless both the switch and endpoint are set.
VITE_TELEMETRY_ENABLED=false
VITE_TELEMETRY_ENDPOINT=
VITE_TELEMETRY_SITE_ID=
VITE_TELEMETRY_SAMPLE_RATE=1

# Browser variables are public. Never place API secrets in VITE_* values.
